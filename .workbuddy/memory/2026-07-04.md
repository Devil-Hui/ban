# 2026-07-04 工作日志

## 排班小程序 v3.1 更新

### 10 幕真实推演验证
- 模拟"小明(班长)+小红(同学)"完整生命周期推演
- 逐帧对照 10 幕操作×11 张表读写，发现并修复 3 个缺口

### 新增内容
- tasks 表新增 `previous_schemes`（adjusting 状态回滚备份）
- tasks 表新增 `share_token`（预览防爬）
- 系统架构四层分离（表现层/接口层/服务层/数据层）
- 软删除重入逻辑确认 + 伪代码
- 首页分组卡片查询流（JOIN 联表）
- AI 视觉识别策略（云端视觉小模型 + 手动拖拽降级）

### 关键决策
- 软删除：保留 soft-delete，不用物理删除
- 模型：云端优先（视觉小模型），本地降级（手动拖拽）
- 模型存放：云存储 bucket，云函数 require 路径加载
- group_members 唯一索引：(group_id, user_id) 联合唯一
- task_responses 唯一索引：(task_id, user_id) 联合唯一

### Commit
- `docs(v3.1): 10幕推演验证+架构四层分离+软删除确认+AI视觉模型策略`
